<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script>
        let name = opener.document.getElementById("ID_reg").value;
        let chk = true;
        window.onload=function(){
            document.getElementById("dup_id").value = name;
        }
        function sendclose(){
            if(!chk){
                alert(`중복 확인 해라`);
            }else{
                opener.document.getElementById("ID_reg").value = document.getElementById("dup_id").value;
                window.close();
            }
        }
    </script>
</head>
<body>
    <form action="/register/dup_chk" method="POST">
        <div>
            <label for="dup_id">아이디 : </label>
            <input type="text" name="dup_id" id="dup_id">
            <input type="submit" value="중복체크" onclick="send();">
        </div>
    </form>
    <div>
        <button onclick="sendclose();">사용하기</button>
        <button onclick="window.close();">닫기</button>
    </div>
</body>
</html>
